services:
  main:
    build:
      context: .
      dockerfile: Dockerfile
    image: ad-rpa:local
    container_name: ad-rpa-main
    env_file:
      - .env
    command: ["--apply"]
    restart: unless-stopped
